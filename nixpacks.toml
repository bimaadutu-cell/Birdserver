# Nixpacks configuration for Railway / other Nixpacks-based platforms.
# Node 20 LTS + build + start.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm ci --include=dev", "npm run build"]

[start]
cmd = "npm start"

# Nixpacks configuration for Railway / other Nixpacks-based platforms.
# Node 22 LTS + build + start.

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm start"
